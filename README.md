# clip-dl

This is the repository for the clip-dl web extension, which allows users to easily download clips from YouTube videos by selecting a time range and format. The extension uses native messaging to communicate with a Python host script that runs `yt-dlp` and `ffmpeg` to perform the actual downloading and clipping of the video.
